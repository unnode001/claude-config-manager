# Clippy configuration for Claude Config Manager
# Simplified configuration for compatibility with newer clippy versions

# Enable pedantic lints (more strict checks)
# These are now enabled via command-line: cargo clippy -- -W clippy::pedantic
